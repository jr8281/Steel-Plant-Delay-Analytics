# Backend API Configuration
API_BASE_URL = "http://localhost:8000"

# Database (optional - for local testing only)
DATABASE_URL = "postgresql+psycopg2://steeluser:password@localhost:5432/steelplant"

# API Keys
GROQ_API_KEY = "your_groq_api_key_here"

# JWT Secret (if frontend needs it)
JWT_SECRET = "your_jwt_secret_here"